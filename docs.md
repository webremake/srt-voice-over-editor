# Техническая документация: SRT Voice-over Editor

Эта документация содержит глубокое описание архитектуры, алгоритмов и проектных решений, использованных в проекте.

---

## 🏗 Архитектура решения

Проект представляет собой пайплайн обработки текста, где **LLM (Gemini 3 Flash)** выступает в роли интеллектуального редактора, а библиотека **pysrt** гарантирует сохранение структуры и таймкодов.

### Пайплайн данных:
1. **Загрузка и парсинг**: Файл .srt загружается через `pysrt`. Каждый блок (таймкод + текст) становится отдельным объектом.
2. **Батчинг (Группировка)**: Объекты группируются по 50 штук. Это оптимальный размер для контекстного окна Gemini 3 Flash, позволяющий модели видеть контекст соседних строк.
3. **Промпт-инжиниринг**: Батч преобразуется в нумерованный список. Это критически важно для того, чтобы модель вернула 1:1 ответ.
4. **Редактирование (LLM)**: Модель сжимает текст, удаляя вводные слова и упрощая синтаксис для озвучки.
5. **Валидация и "Прошивка"**: Полученный текст сверяется с исходным количеством блоков. Если всё верно — текст записывается в объекты `pysrt`.
6. **Сохранение**: Библиотека `pysrt` собирает финальный файл, сохраняя оригинальные микросекунды и форматирование.

---

## 🔄 Стратегия восстановления (Retry Logic)

Одна из главных проблем при работе с LLM в пакетном режиме — "галлюцинации" структуры (модель может объединить две строки в одну или пропустить строку).

Для борьбы с этим внедрена **двухуровневая каскадная система ретраев**:

1. **Уровень 1 (50 строк)**: Основной рабочий режим.
2. **Уровень 2 (10 строк)**: Если большой батч вернул неверное количество строк, этот участок (50 строк) разбивается на 5 порций по 10 строк. Модели проще удерживать структуру на меньшем объеме.
3. **Уровень 3 (5 строк)**: Если батч из 10 строк не проходит, он делится на две порции по 5 строк.
4. **Fallback**: Если даже 5 строк не редактируются корректно — скрипт оставляет оригинальный текст, чтобы не нарушать целостность SRT-файла.

---

## ⏱ Лимиты и Rate Limiting

Проект оптимизирован для работы на **Free Tier Gemini API**. 
Лимит составляет **5 RPM (запросов в минуту)**.

**Реализация**:
- После каждого вызова API (`process_batch`) в основном цикле и во всех уровнях ретраев стоит принудительная задержка **15 секунд**.
- Это гарантирует, что даже при глубоком каскадном ретрае скрипт не получит ошибку `429 Resource Exhausted`.

---

## 🧠 Промпт-инжиниринг

Системный промпт (`SYSTEM_PROMPT`) разработан с использованием техник *Chain of Thought* и явных ограничений:

- **Нумерованный список**: Гарантирует структурированный ответ, который легко парсить регулярными выражениями.
- **Запрет перевода**: Жесткое требование сохранять исходный язык (Русский).
- **Сохранение сущностей**: Явное указание не трогать имена собственные, бренды и технические термины — это критично для образовательного контента.

---

## 🛠 Зависимости

1. **`google-generativeai`**: Официальный клиент для Gemini API.
2. **`pysrt`**: Стандарт индустрии для работы с SRT. В отличие от простых регулярных выражений, корректно обрабатывает сложные случаи форматирования таймкодов.
3. **`python-dotenv`**: Для безопасного управления API ключами вне кода.
